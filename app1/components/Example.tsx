import { FC } from "react";

const Example: FC = () => {
  return (
    <div style={{ width: 500, height: 500, background: "red", color: "white" }}>
      HEllo world
    </div>
  );
};

export default Example;
